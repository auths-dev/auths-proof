# Assurance fixtures

This directory holds deterministic public inputs needed to reproduce assurance
verification for the exact candidate. It must never contain provider
credentials, recovery references, protected action bytes, private receipt
disclosures, private keys, or raw customer identity data.

No candidate-bound fixtures have been recorded yet.
