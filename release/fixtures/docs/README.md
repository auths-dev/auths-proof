# Documentation bundle fixture

`cargo xtask docs-bundle target/docs-bundle` builds the maintained fixture from
the current documentation contract and installed-surface snapshots. The bundle
manifest is strict, bounded, checksum-addressed, and tied to the source commit.
