# Debugging

Check `/healthz`, `/readyz`, and `/api/v1/scenario`. Denials must report zero credential requests and provider calls. Unknown outcomes must retain all recurring liability until reconciliation proves the provider result.
