# Architecture

The API models proof and policy evaluation, a durable cancellation release intent, an exact claim, a cancellation-only credential boundary, provider observation, and liability accounting. `SubscriptionCancelReceipt` is a closed profile-owned receipt family.
