const id=location.pathname.split("/").filter(Boolean).pop();const api=(window.AUTHS_SUBSCRIPTION_CREATE_API_BASE||"").replace(/\/$/,"");document.getElementById("id").textContent=id.slice(0,12)+"…";fetch(`${api}/api/v1/receipts/${id}`).then(async r=>{const v=await r.json();if(!r.ok)throw new Error(v.error||r.statusText);document.getElementById("summary").textContent="This record binds Auths authority, the exact mandate, Price, provider-clock term, reservations, credential boundary, and observed Stripe lifecycle.";document.getElementById("receipt").textContent=JSON.stringify(v,null,2)}).catch(e=>{document.getElementById("summary").textContent=e.message});
