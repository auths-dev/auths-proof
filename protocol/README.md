# Route contracts

`github-issue-address-v2.json` is a specified route contract for the
`auths.github.issue-address/2` control plane. No server or client in this
repository implements its routes; `auths-github` executes the
`auths.github.issue-address/1` action domain.
