schema = 1

[layers.core]
path = "core/"
allowed_dependencies = ["core"]
owners = ["@auths-dev/core-maintainers", "@auths-dev/security"]

[layers.exchange]
path = "exchange/"
allowed_dependencies = ["core", "exchange"]
owners = ["@auths-dev/exchange-maintainers", "@auths-dev/security"]

[layers.product]
path = "product/"
allowed_dependencies = ["core", "exchange", "product"]
owners = ["@auths-dev/product-maintainers", "@auths-dev/security"]

[layers.bindings]
path = "bindings/"
allowed_dependencies = ["core", "exchange", "product", "bindings"]
owners = ["@auths-dev/binding-maintainers", "@auths-dev/security"]

[layers.demos]
path = "demos/"
allowed_dependencies = ["core", "exchange", "product", "bindings", "demos"]
owners = ["@auths-dev/product-maintainers"]

[layers.tooling]
path = "xtask"
allowed_dependencies = ["core", "exchange", "product", "bindings", "demos", "tooling"]
owners = ["@auths-dev/architecture", "@auths-dev/security"]

[policy]
workspace_edition = "2024"
workspace_resolver = "3"
workspace_msrv = "1.91"
development_toolchain = "1.97.1"
core_forbidden_dependencies = [
  "actix",
  "axum",
  "aws-config",
  "azure_core",
  "diesel",
  "git2",
  "hyper",
  "iroh",
  "keyring",
  "quinn",
  "reqwest",
  "rmcp",
  "rusqlite",
  "sea-orm",
  "sqlx",
  "tokio",
  "tonic",
  "warp",
]
core_default_feature_exceptions = ["libfuzzer-sys", "rcgen"]
approved_build_scripts = []
approved_native_dependencies = []
no_std_packages = [
  "auths",
  "auths-algebra-kernel",
  "auths-assurance",
  "auths-author",
  "auths-authority",
  "auths-codec",
  "auths-composition",
  "auths-did-keri",
  "auths-did-key",
  "auths-did-web",
  "auths-hsm-attested",
  "auths-identity",
  "auths-identity-authority",
  "auths-identity-raw-key",
  "auths-model",
  "auths-multikey",
  "auths-ports",
  "auths-proof",
  "auths-raw-key",
  "auths-raw-key-core",
  "auths-registries",
  "auths-signature",
  "auths-signature-core",
  "auths-signature-ed25519",
  "auths-spiffe-x509",
  "auths-verifier",
  "auths-webauthn",
]

[packages]
auths-algebra-kernel = "core"
auths-apps-testkit = "demos"
auths-identity = "core"
auths-identity-authority = "core"
auths-identity-raw-key = "core"
auths-identity-iroh-demo = "demos"
auths-iroh = "exchange"
auths-bench-model = "demos"
auths-bench-native = "demos"
auths-bench-wasm = "demos"
auths-byte-channel = "exchange"
auths-byte-channel-memory = "exchange"
auths-assurance = "core"
auths-author = "core"
auths-authority = "core"
auths-cache = "product"
auths-ci-plan = "tooling"
auths-codec = "core"
auths-composition = "core"
auths-cross-company-incident-edgeshield-demo = "demos"
auths-formal-refinement = "core"
auths-github = "product"
auths-config = "product"
auths-custody = "product"
auths-custody-aws-kms = "product"
auths-custody-pkcs11 = "product"
auths-did-keri = "core"
auths-did-key = "core"
auths-did-web = "core"
auths-enforcement = "product"
auths-errors = "product"
auths-evidence-assemblers = "product"
auths-hsm-attested = "core"
auths-lab-matrix = "demos"
auths-lab-wasm-bench = "demos"
auths-live-lab = "demos"
auths-live-service = "demos"
auths-mcp-demo = "demos"
auths-model = "core"
auths-multikey = "core"
auths-operations = "product"
auths-operations-otel = "product"
auths-opentofu = "product"
auths-opentofu-demo = "demos"
auths-postgresql = "product"
auths-postgresql-demo = "demos"
auths-records-api = "product"
auths-records-demo = "demos"
auths-explain = "product"
auths-ports = "core"
auths-profile-api = "product"
auths-profile-domains = "product"
auths-profile-kit = "product"
auths-qualification-evidence-source = "product"
auths-qualification-supervisor = "product"
auths-bounded-policy = "product"
auths-bounded-policy-fuzz = "product"
auths-lifecycle = "product"
auths-profile-mcp = "product"
auths-proof = "core"
auths-proof-fuzz = "core"
auths-proof-exchange = "exchange"
auths-proof-exchange-codec = "exchange"
auths-proof-exchange-file = "exchange"
auths-proof-exchange-framing = "exchange"
auths-proof-exchange-https = "exchange"
auths-proof-exchange-iroh = "exchange"
auths-proof-exchange-memory = "exchange"
auths-proof-exchange-model = "exchange"
auths-proof-exchange-port = "exchange"
auths-proof-exchange-tcp = "exchange"
auths-proof-exchange-testkit = "exchange"
auths-proof-exchange-unix = "exchange"
auths-proof-offline-example = "demos"
auths-proof-python = "bindings"
auths-proof-wasm = "bindings"
auths-raw-key = "core"
auths-raw-key-core = "core"
auths-radicle = "product"
auths-radicle-demo = "demos"
auths-stripe = "product"
auths-stripe-connect-transfer-demo = "demos"
auths-stripe-demo = "demos"
auths-stripe-payout-demo = "demos"
auths-stripe-payment-demo-common = "demos"
auths-stripe-payment-collect-demo = "demos"
auths-stripe-payment-authorize-demo = "demos"
auths-stripe-payment-cancel-demo = "demos"
auths-stripe-payment-mandate-demo = "demos"
auths-stripe-purchase-authorization-demo = "demos"
auths-stripe-subscription-cancel-demo = "demos"
auths-stripe-subscription-create-demo = "demos"
auths-stripe-subscription-modify-demo = "demos"
auths-stripe-payment-capture-demo = "demos"
auths-kubernetes = "product"
auths-kubernetes-demo = "demos"
auths-kernel-runtime = "product"
auths-receipts = "product"
auths-registries = "core"
auths-resolver-did-web = "product"
auths-production-client = "product"
auths-connections = "product"
auths-profile-runtime = "product"
auths-node = "product"
auths-runtime = "product"
auths = "core"
auths-sdk = "product"
auths-signature = "core"
auths-signature-core = "core"
auths-signature-ed25519 = "core"
auths-spiffe-x509 = "core"
auths-stores = "product"
auths-testkit = "core"
auths-verifier = "core"
auths-webauthn = "core"
xtask = "tooling"

[dependency_boundaries.identity]
roots = ["auths-identity"]
allowed_workspace_dependencies = []

[dependency_boundaries.identity-adapters]
roots = [
  "auths-identity-authority",
  "auths-identity-raw-key",
  "auths-signature-ed25519",
]
allowed_workspace_dependencies = ["auths-identity", "auths-identity-raw-key", "auths-model", "auths-raw-key-core", "auths-signature-core"]

[dependency_boundaries.iroh-transport]
roots = ["auths-iroh"]
allowed_workspace_dependencies = ["auths-byte-channel"]

[dependency_boundaries.byte-channel]
roots = ["auths-byte-channel"]
allowed_workspace_dependencies = []

[dependency_boundaries.byte-channel-adapters]
roots = ["auths-byte-channel-memory"]
allowed_workspace_dependencies = ["auths-byte-channel"]

[dependency_boundaries.kernel-runtime]
roots = ["auths-kernel-runtime"]
allowed_workspace_dependencies = [
  "auths-algebra-kernel",
  "auths-assurance",
  "auths-author",
  "auths-authority",
  "auths-codec",
  "auths-composition",
  "auths-did-keri",
  "auths-did-key",
  "auths-did-web",
  "auths-hsm-attested",
  "auths-identity",
  "auths-identity-authority",
  "auths-identity-raw-key",
  "auths-model",
  "auths-multikey",
  "auths-ports",
  "auths-raw-key",
  "auths-raw-key-core",
  "auths-registries",
  "auths-signature",
  "auths-signature-core",
  "auths-signature-ed25519",
  "auths-spiffe-x509",
  "auths-testkit",
  "auths-verifier",
  "auths-webauthn",
]

[exceptions]

# Bindings call the Rust core and the WASM authoring ABI for every protocol
# meaning. A cryptographic primitive or protocol constant written directly in
# binding source is a second opinion about what Auths means, and each new
# language multiplies it. Anything genuinely local is declared below with a
# written reason; a stale allowance fails the check as loudly as an
# undeclared primitive.
[binding_semantics]
scanned_paths = [
  "bindings/typescript/src",
  "bindings/python/python",
]
# Independent implementations exist to reproduce semantics on purpose.
exempt_paths = ["bindings/independent"]
scanned_extensions = ["ts", "js", "mjs", "py"]
forbidden_patterns = [
  "crypto.subtle",
  "getRandomValues",
  "createHash",
  "createHmac",
  "createPrivateKey",
  "createPublicKey",
  "hashlib",
  "hmac.",
  "os.urandom",
  "secrets.",
  "nacl.",
  "cryptography.hazmat",
  # A binding that frames its own bytes is defining a wire format the other
  # SDKs must reproduce exactly. Canonical encodings belong to the Rust core.
  "JSON.stringify",
  "DataView",
]

[[binding_semantics.allowances]]
path = "bindings/typescript/src/internal/development.ts"
pattern = "crypto.subtle"
status = "permanent"
reason = "Non-production local signer and receipt-attestor keys are mechanism state, while Rust owns every signed preimage and verification meaning."

[[binding_semantics.allowances]]
path = "bindings/typescript/src/internal/receipt-attestation.ts"
pattern = "JSON.stringify"
status = "permanent"
reason = "The development receipt signer serializes a bounded private attestation envelope around Rust-owned receipt claims and is not a protocol codec."

[[binding_semantics.allowances]]
path = "bindings/typescript/src/session.ts"
pattern = "getRandomValues"
status = "permanent"
reason = "The local transport creates a fresh correlation request ID; the agent validates it only for response binding and it grants no authority or provider access."

[[binding_semantics.allowances]]
path = "bindings/typescript/src/profile-runtime.ts"
pattern = "getRandomValues"
status = "permanent"
reason = "A generated client creates a fresh correlation request ID; Rust owns all action commitments, idempotency meaning, effect classification, and recovery semantics."

[[binding_semantics.allowances]]
path = "bindings/typescript/src/profile-runtime.ts"
pattern = "crypto.subtle"
status = "permanent"
reason = "The digest is only an in-process coalescing-table key over already canonical input; it is never sent, persisted, signed, or used to authorize provider access."

[[binding_semantics.allowances]]
path = "bindings/typescript/src/testkit/index.ts"
pattern = "crypto.subtle"
status = "permanent"
reason = "The testkit derives deterministic fixture commitments and temporary conformance identifiers; these values provide test isolation and carry no production authority."

[[binding_semantics.allowances]]
path = "bindings/python/python/auths/_public.py"
pattern = "hashlib"
status = "permanent"
reason = "Runtime diagnostics hash the generated Rust-owned error registry artifact for negotiation; receipt commitments themselves are computed and checked by native Rust."

[[binding_semantics.allowances]]
path = "bindings/python/python/auths/identity/authoring.py"
pattern = "hashlib"
status = "permanent"
reason = "The raw-key authoring convenience derives the display identity identifier from the same public key whose canonical descriptor is constructed and validated by native Rust."

[[binding_semantics.allowances]]
path = "bindings/python/python/auths/_session.py"
pattern = "os.urandom"
status = "permanent"
reason = "The local transport creates a fresh correlation request ID; the agent validates it only for response binding and it grants no authority or provider access."

[[binding_semantics.allowances]]
path = "bindings/python/python/auths/profile_runtime.py"
pattern = "os.urandom"
status = "permanent"
reason = "A generated client creates a fresh correlation request ID; Rust owns all action commitments, idempotency meaning, effect classification, and recovery semantics."

[[binding_semantics.allowances]]
path = "bindings/python/python/auths/profile_runtime.py"
pattern = "hashlib"
status = "permanent"
reason = "The digest is only an in-process coalescing-table key over already canonical input; it is never sent, persisted, signed, or used to authorize provider access."

[[binding_semantics.allowances]]
path = "bindings/python/python/auths/testkit.py"
pattern = "hashlib"
status = "permanent"
reason = "The testkit hashes a timestamp solely to create an isolated conformance-store name; the value is test mechanism state and carries no authorization meaning."

[[binding_semantics.allowances]]
path = "bindings/python/python/auths/verify.py"
pattern = "hashlib"
status = "permanent"
reason = "The verifier computes redacted host inspection digests after native verification; these diagnostic projections neither authorize an action nor alter the native decision."

[[binding_semantics.allowances]]
path = "bindings/typescript/src/profiles/application/index.ts"
pattern = "getRandomValues"
status = "permanent"
reason = "Fresh local entropy for one action challenge. Randomness cannot come from an effect-free kernel; the authoring ABI receives the challenge and rejects an invalid one, so Rust still owns the rule."
