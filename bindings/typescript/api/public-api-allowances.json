{
  "_comment": [
    "Declared exceptions to the public-API shape rules enforced by",
    "tools/public-api.mjs --shape.",
    "",
    "mirror   one entry point exporting both `X` and `<Prefix>X` of the same kind.",
    "homonym  one name exported from two entry points resolving to different declarations.",
    "",
    "Every entry needs a reason. An allowance that stops matching anything fails the",
    "check, so stale exemptions cannot accumulate silently. Prefer fixing the surface",
    "over adding an entry here: each one is a place a reader must be told which of two",
    "similarly-named things they want."
  ],
  "mirror": [
    {
      "key": "./verify\tReceiptVerificationInput",
      "reason": "VerificationInput is the authority proof/action/context tuple; ReceiptVerificationInput is the distinct signed-receipt plus trust-policy operation selected by the v2 API contract."
    },
    {
      "key": "./self-hosted\tArrayField",
      "reason": "Field is the discriminated union of schema nodes; ArrayField is one concrete node, not a second spelling of Field."
    },
    {
      "key": "./self-hosted\tBooleanField",
      "reason": "Field is the discriminated union of schema nodes; BooleanField is one concrete node, not a second spelling of Field."
    },
    {
      "key": "./self-hosted\tBytesField",
      "reason": "Field is the discriminated union of schema nodes; BytesField is one concrete node, not a second spelling of Field."
    },
    {
      "key": "./self-hosted\tEnumField",
      "reason": "Field is the discriminated union of schema nodes; EnumField is one concrete node, not a second spelling of Field."
    },
    {
      "key": "./self-hosted\tIntegerField",
      "reason": "Field is the discriminated union of schema nodes; IntegerField is one concrete node, not a second spelling of Field."
    },
    {
      "key": "./self-hosted\tObjectField",
      "reason": "Field is the discriminated union of schema nodes; ObjectField is one concrete node, not a second spelling of Field."
    },
    {
      "key": "./self-hosted\tOptionalField",
      "reason": "Field is the discriminated union of schema nodes; OptionalField is one concrete node, not a second spelling of Field."
    },
    {
      "key": "./self-hosted\tStringField",
      "reason": "Field is the discriminated union of schema nodes; StringField is one concrete node, not a second spelling of Field."
    }
  ],
  "homonym": []
}
