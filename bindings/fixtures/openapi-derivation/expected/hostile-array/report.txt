REJECTED  contract
  contract.derive.compiler-limit  #/paths/~1array/post/requestBody/content/application~1json/schema/properties/tags
    tags is an array; the gateway recipe compiler accepts only root-level scalar fields, so no item bound can admit it; no override exists
nothing written
