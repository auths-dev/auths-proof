// generated by auths-profile-generator/1; source=product/integrations/auths-stripe/profile-package.json; sha256=22dd7af4d2d31cfc645300b5ef3fc0977c9bf27821ed04531cf21ceffa1dfa96
//! Generated restricted caller DTOs and deterministic-CBOR codecs.

#![allow(
    clippy::deref_addrof,
    clippy::let_and_return,
    clippy::manual_range_contains,
    clippy::missing_errors_doc,
    clippy::must_use_candidate,
    clippy::needless_borrow,
    clippy::trivially_copy_pass_by_ref,
    clippy::unreadable_literal,
    clippy::useless_conversion
)]

#[allow(unused_imports)]
use minicbor::{Decoder, Encoder, data::Type};
use std::collections::BTreeMap;

/// Exact validated `auths.profile-api/1` JSON.
pub const PROFILE_API_JSON: &str = r#"{"schema":"auths.profile-api/1","types":{"Currency":{"kind":"enum","values":["eur","gbp","usd"]},"Refund":{"kind":"record","fields":[{"name":"id","value":{"kind":"string","minimumBytes":1,"maximumBytes":128,"alphabet":"registered-token"},"sensitive":false},{"name":"status","value":{"kind":"enum","values":["pending","succeeded"]},"sensitive":false}]},"RefundInput":{"kind":"record","fields":[{"name":"paymentIntent","value":{"kind":"string","minimumBytes":1,"maximumBytes":128,"alphabet":"registered-token"},"sensitive":false},{"name":"amount","value":{"kind":"uint","bits":64,"minimum":"1","maximum":"100000000"},"sensitive":false},{"name":"currency","value":{"kind":"ref","name":"Currency"},"sensitive":false}]}}}"#;

/// Closed generated profile-API codec failure.
#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub enum ProfileApiCodecError {
    Malformed,
    NonCanonical,
    Limit,
}

impl core::fmt::Display for ProfileApiCodecError {
    fn fmt(&self, formatter: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        formatter.write_str(match self {
            Self::Malformed => "malformed generated profile value",
            Self::NonCanonical => "non-canonical generated profile value",
            Self::Limit => "generated profile value exceeds its bound",
        })
    }
}

impl std::error::Error for ProfileApiCodecError {}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub enum Currency {
    Eur,
    Gbp,
    Usd,
}

impl Currency {
    pub const fn as_str(self) -> &'static str {
        match self {
            Self::Eur => "eur",
            Self::Gbp => "gbp",
            Self::Usd => "usd",
        }
    }

    pub fn to_canonical_cbor(&self) -> Result<Vec<u8>, ProfileApiCodecError> {
        let mut encoder = Encoder::new(Vec::new());
        self.encode_into(&mut encoder)?;
        Ok(encoder.into_writer())
    }

    pub fn from_canonical_cbor(bytes: &[u8]) -> Result<Self, ProfileApiCodecError> {
        let mut decoder = Decoder::new(bytes);
        let value = Self::decode_from(&mut decoder)?;
        finish(&decoder, bytes)?;
        if value.to_canonical_cbor()?.as_slice() != bytes {
            return Err(ProfileApiCodecError::NonCanonical);
        }
        Ok(value)
    }

    fn encode_into(&self, encoder: &mut Encoder<Vec<u8>>) -> Result<(), ProfileApiCodecError> {
        encoder.str(self.as_str()).map_err(codec)?;
        Ok(())
    }

    fn decode_from(decoder: &mut Decoder<'_>) -> Result<Self, ProfileApiCodecError> {
        match decoder.str().map_err(codec)? {
            "eur" => Ok(Self::Eur),
            "gbp" => Ok(Self::Gbp),
            "usd" => Ok(Self::Usd),
            _ => Err(ProfileApiCodecError::Malformed),
        }
    }
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub struct Refund {
    pub id: String,
    pub status: RefundStatus,
}

impl Refund {
    pub fn to_canonical_cbor(&self) -> Result<Vec<u8>, ProfileApiCodecError> {
        let mut encoder = Encoder::new(Vec::new());
        self.encode_into(&mut encoder)?;
        Ok(encoder.into_writer())
    }

    pub fn from_canonical_cbor(bytes: &[u8]) -> Result<Self, ProfileApiCodecError> {
        let mut decoder = Decoder::new(bytes);
        let value = Self::decode_from(&mut decoder)?;
        finish(&decoder, bytes)?;
        if value.to_canonical_cbor()?.as_slice() != bytes {
            return Err(ProfileApiCodecError::NonCanonical);
        }
        Ok(value)
    }

    fn encode_into(&self, encoder: &mut Encoder<Vec<u8>>) -> Result<(), ProfileApiCodecError> {
        encoder.map(2).map_err(codec)?;
        encoder.str("id").map_err(codec)?;
        if !valid_string(&self.id, 1usize, 128usize, "registered-token") {
            return Err(ProfileApiCodecError::Limit);
        }
        encoder.str(&self.id).map_err(codec)?;
        encoder.str("status").map_err(codec)?;
        (&self.status).encode_into(encoder)?;
        Ok(())
    }

    fn decode_from(decoder: &mut Decoder<'_>) -> Result<Self, ProfileApiCodecError> {
        exact_map(decoder, 2)?;
        expect_key(decoder, "id")?;
        let id = {
            let value = decoder.str().map_err(codec)?.to_owned();
            if !valid_string(&value, 1usize, 128usize, "registered-token") {
                return Err(ProfileApiCodecError::Limit);
            }
            value
        };
        expect_key(decoder, "status")?;
        let status = RefundStatus::decode_from(decoder)?;
        Ok(Self { id, status })
    }
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub struct RefundInput {
    pub payment_intent: String,
    pub amount: u64,
    pub currency: Currency,
}

impl RefundInput {
    pub fn to_canonical_cbor(&self) -> Result<Vec<u8>, ProfileApiCodecError> {
        let mut encoder = Encoder::new(Vec::new());
        self.encode_into(&mut encoder)?;
        Ok(encoder.into_writer())
    }

    pub fn from_canonical_cbor(bytes: &[u8]) -> Result<Self, ProfileApiCodecError> {
        let mut decoder = Decoder::new(bytes);
        let value = Self::decode_from(&mut decoder)?;
        finish(&decoder, bytes)?;
        if value.to_canonical_cbor()?.as_slice() != bytes {
            return Err(ProfileApiCodecError::NonCanonical);
        }
        Ok(value)
    }

    fn encode_into(&self, encoder: &mut Encoder<Vec<u8>>) -> Result<(), ProfileApiCodecError> {
        encoder.map(3).map_err(codec)?;
        encoder.str("amount").map_err(codec)?;
        if !(1u64..=100000000u64).contains(&self.amount) {
            return Err(ProfileApiCodecError::Limit);
        }
        encoder.u64(u64::from(*&self.amount)).map_err(codec)?;
        encoder.str("currency").map_err(codec)?;
        (&self.currency).encode_into(encoder)?;
        encoder.str("paymentIntent").map_err(codec)?;
        if !valid_string(&self.payment_intent, 1usize, 128usize, "registered-token") {
            return Err(ProfileApiCodecError::Limit);
        }
        encoder.str(&self.payment_intent).map_err(codec)?;
        Ok(())
    }

    fn decode_from(decoder: &mut Decoder<'_>) -> Result<Self, ProfileApiCodecError> {
        exact_map(decoder, 3)?;
        expect_key(decoder, "amount")?;
        let amount = {
            let value = decoder.u64().map_err(codec)?;
            if value < 1u64 || value > 100000000u64 {
                return Err(ProfileApiCodecError::Limit);
            }
            value
        };
        expect_key(decoder, "currency")?;
        let currency = Currency::decode_from(decoder)?;
        expect_key(decoder, "paymentIntent")?;
        let payment_intent = {
            let value = decoder.str().map_err(codec)?.to_owned();
            if !valid_string(&value, 1usize, 128usize, "registered-token") {
                return Err(ProfileApiCodecError::Limit);
            }
            value
        };
        Ok(Self {
            payment_intent,
            amount,
            currency,
        })
    }
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub enum RefundStatus {
    Pending,
    Succeeded,
}

impl RefundStatus {
    pub const fn as_str(self) -> &'static str {
        match self {
            Self::Pending => "pending",
            Self::Succeeded => "succeeded",
        }
    }

    pub fn to_canonical_cbor(&self) -> Result<Vec<u8>, ProfileApiCodecError> {
        let mut encoder = Encoder::new(Vec::new());
        self.encode_into(&mut encoder)?;
        Ok(encoder.into_writer())
    }

    pub fn from_canonical_cbor(bytes: &[u8]) -> Result<Self, ProfileApiCodecError> {
        let mut decoder = Decoder::new(bytes);
        let value = Self::decode_from(&mut decoder)?;
        finish(&decoder, bytes)?;
        if value.to_canonical_cbor()?.as_slice() != bytes {
            return Err(ProfileApiCodecError::NonCanonical);
        }
        Ok(value)
    }

    fn encode_into(&self, encoder: &mut Encoder<Vec<u8>>) -> Result<(), ProfileApiCodecError> {
        encoder.str(self.as_str()).map_err(codec)?;
        Ok(())
    }

    fn decode_from(decoder: &mut Decoder<'_>) -> Result<Self, ProfileApiCodecError> {
        match decoder.str().map_err(codec)? {
            "pending" => Ok(Self::Pending),
            "succeeded" => Ok(Self::Succeeded),
            _ => Err(ProfileApiCodecError::Malformed),
        }
    }
}

fn codec<T>(_: T) -> ProfileApiCodecError {
    ProfileApiCodecError::Malformed
}

fn exact_map(decoder: &mut Decoder<'_>, expected: usize) -> Result<(), ProfileApiCodecError> {
    let count = decoder
        .map()
        .map_err(codec)?
        .ok_or(ProfileApiCodecError::Malformed)?;
    if usize::try_from(count).ok() != Some(expected) {
        return Err(ProfileApiCodecError::Malformed);
    }
    Ok(())
}

#[allow(dead_code)]
fn exact_array(
    decoder: &mut Decoder<'_>,
    minimum: usize,
    maximum: usize,
) -> Result<usize, ProfileApiCodecError> {
    let count = decoder
        .array()
        .map_err(codec)?
        .ok_or(ProfileApiCodecError::Malformed)?;
    let count = usize::try_from(count).map_err(|_| ProfileApiCodecError::Limit)?;
    if count < minimum || count > maximum {
        return Err(ProfileApiCodecError::Limit);
    }
    Ok(count)
}

fn expect_key(decoder: &mut Decoder<'_>, expected: &str) -> Result<(), ProfileApiCodecError> {
    if decoder.str().map_err(codec)? != expected {
        return Err(ProfileApiCodecError::Malformed);
    }
    Ok(())
}

fn finish(decoder: &Decoder<'_>, bytes: &[u8]) -> Result<(), ProfileApiCodecError> {
    if decoder.position() != bytes.len() {
        return Err(ProfileApiCodecError::Malformed);
    }
    Ok(())
}

fn valid_string(value: &str, minimum: usize, maximum: usize, alphabet: &str) -> bool {
    let bytes = value.as_bytes();
    if bytes.len() < minimum || bytes.len() > maximum {
        return false;
    }
    match alphabet {
        "utf8" => !value
            .chars()
            .any(|character| matches!(character as u32, 0..=31 | 127..=159)),
        "ascii-graphic" => bytes.iter().all(|byte| matches!(byte, 0x21..=0x7e)),
        "registered-token" => {
            bytes.first().is_some_and(u8::is_ascii_alphanumeric)
                && bytes.iter().all(|byte| {
                    byte.is_ascii_alphanumeric() || matches!(byte, b'.' | b'_' | b':' | b'-')
                })
        }
        "lower-token" => {
            bytes.first().is_some_and(u8::is_ascii_lowercase)
                && bytes
                    .iter()
                    .all(|byte| byte.is_ascii_lowercase() || byte.is_ascii_digit() || *byte == b'-')
        }
        "lower-hex" => {
            !bytes.is_empty()
                && bytes
                    .iter()
                    .all(|byte| byte.is_ascii_digit() || matches!(byte, b'a'..=b'f'))
        }
        "base64url" => {
            !bytes.is_empty()
                && bytes
                    .iter()
                    .all(|byte| byte.is_ascii_alphanumeric() || matches!(byte, b'_' | b'-'))
        }
        _ => false,
    }
}

#[allow(dead_code)]
fn decode_union_fields(
    decoder: &mut Decoder<'_>,
    maximum: usize,
) -> Result<BTreeMap<String, Vec<u8>>, ProfileApiCodecError> {
    let count = decoder
        .map()
        .map_err(codec)?
        .ok_or(ProfileApiCodecError::Malformed)?;
    let count = usize::try_from(count).map_err(|_| ProfileApiCodecError::Limit)?;
    if count == 0 || count > maximum {
        return Err(ProfileApiCodecError::Limit);
    }
    let mut fields = BTreeMap::new();
    for _ in 0..count {
        let key = decoder.str().map_err(codec)?.to_owned();
        let start = decoder.position();
        decoder.skip().map_err(codec)?;
        let end = decoder.position();
        if fields
            .insert(key, decoder.input()[start..end].to_vec())
            .is_some()
        {
            return Err(ProfileApiCodecError::Malformed);
        }
    }
    Ok(fields)
}

#[allow(dead_code)]
fn decode_isolated<T>(
    bytes: &[u8],
    decode: impl FnOnce(&mut Decoder<'_>) -> Result<T, ProfileApiCodecError>,
) -> Result<T, ProfileApiCodecError> {
    let mut decoder = Decoder::new(bytes);
    let value = decode(&mut decoder)?;
    finish(&decoder, bytes)?;
    Ok(value)
}
