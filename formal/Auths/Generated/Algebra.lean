-- @generated by `cargo xtask formal --update`; DO NOT EDIT.

namespace Auths.Generated

def contractSchema : String := "auths-proof-algebra-contract/v1"

def exhaustiveThresholdBound : Nat := 16

inductive Truth where
  | denied
  | indeterminate
  | authorized
  deriving BEq, DecidableEq, Repr

structure AttenuationProjection where
  rootPreserved : Bool
  depthDecreases : Bool
  profileAttenuates : Bool
  permissionsAttenuate : Bool
  validityAttenuates : Bool
  audiencesAttenuate : Bool
  actionConstraintAttenuates : Bool
  budgetAttenuates : Bool
  statusAttenuates : Bool
  assuranceAttenuates : Bool
  extensionsAttenuate : Bool
  deriving BEq, DecidableEq, Repr

def attenuationAccepts (projection : AttenuationProjection) : Bool :=
  projection.rootPreserved &&
  projection.depthDecreases &&
  projection.profileAttenuates &&
  projection.permissionsAttenuate &&
  projection.validityAttenuates &&
  projection.audiencesAttenuate &&
  projection.actionConstraintAttenuates &&
  projection.budgetAttenuates &&
  projection.statusAttenuates &&
  projection.assuranceAttenuates &&
  projection.extensionsAttenuate

def thresholdCounts (required authorized indeterminate : Nat) : Truth :=
  if authorized ≥ required then .authorized
  else if authorized + indeterminate ≥ required then .indeterminate
  else .denied

end Auths.Generated
