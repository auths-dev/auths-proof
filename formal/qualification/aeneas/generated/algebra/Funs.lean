-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [auths_algebra_kernel]: function definitions
import Aeneas
import qualification.aeneas.generated.algebra.Types
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

namespace auths_algebra_kernel

/-- [auths_algebra_kernel::generated::attenuation_checks_accept]:
    Source: 'core/crates/auths-algebra-kernel/src/generated.rs', lines 141:0-153:1
    Visibility: public -/
def generated.attenuation_checks_accept
  (checks : generated.AttenuationChecks) : Result Bool := do
  if checks.root_preserved
  then
    if checks.depth_decreases
    then
      if checks.profile_attenuates
      then
        if checks.permissions_attenuate
        then
          if checks.validity_attenuates
          then
            if checks.audiences_attenuate
            then
              if checks.action_constraint_attenuates
              then
                if checks.budget_attenuates
                then
                  if checks.status_attenuates
                  then
                    if checks.assurance_attenuates
                    then ok checks.extensions_attenuate
                    else ok false
                  else ok false
                else ok false
              else ok false
            else ok false
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

end auths_algebra_kernel
