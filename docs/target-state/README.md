# Historical target-state material

Every document in this directory is retained as planning and review history.
None is normative. The launch contract is the combined set under `spec/v1/`,
the canonical fixture manifest and bytes under `fixtures/v1/`, and the stable
types/codes enforced by `cargo xtask spec-sync`.

When a historical statement conflicts with those artifacts, the normative
launch contract wins.
